const ppl = require('./people');

console.log(ppl);

const os = require('os');

// console.log(os.platform());
// console.log(os.homedir());
// console.log(os.userInfo());

console.log(os.getOSVersion());
// os is an inbuilt node module which is used to access the info about the os